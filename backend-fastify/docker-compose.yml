services:
  database:
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fullstack_auth_fastify_drizzle
    volumes:
      - ./data/postgres:/var/lib/postgresql
    restart: unless-stopped
